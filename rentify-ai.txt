rentify-ai/
├── data/
│   ├── raw/                      # Archivos originales (AppFolio, CSVs sin procesar)
│   ├── clean/                    # Datos normalizados y listos para conciliación
│   ├── master/                   # Dataset consolidado (statements unificados + allocations finales)
├── scripts/
│   ├── ingestion/                # Scripts para leer, limpiar y preparar datos
│   │   ├── normalize_gl_accounts.py
│   │   ├── unify_statements.py        # Unificar todos los statements en uno solo
│   │   └── create_allocations.py      # Crear allocations según reglas de negocio
│   ├── fuzzy_match/              # Scripts para búsquedas aproximadas y conciliación difusa
│   │   └── match_transactions.py      # Ejemplo: emparejar movimientos Amex con notas usando fuzzy matching
│   └── utils/                    # Funciones compartidas (normalización, logging, etc.)
│       └── text_cleaning.py


# Crear los allocations que sean necesarios
#  